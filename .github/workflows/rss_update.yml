name: Update EnglishJobs RSS

on:
  schedule:
    - cron: '0 */6 * * *'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - uses: actions/setup-python@v4
      with:
        python-version: "3.x"

    - run: echo         python -m pip install --upgrade pip 
        pip install requests beautifulsoup4

    - run: python rss/rss_generator.py

    - run: echo         git config --global user.name "github-actions[bot]" 
        git config --global user.email "github-actions[bot]@users.noreply.github.com" 
        git add rss/englishjobs_madrid.xml 
        git commit -m "Actualizar RSS autom ticamente [skip ci]" 
